<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HourSync.Leaderboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HourSync"
    xmlns:core="using:HourSyncCoreLib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:NameMatchConverter x:Key="NameMatchConverter"/>
    </Page.Resources>

    <Grid Margin="36,40">
        <StackPanel Orientation="Vertical" Spacing="10">
            <!-- Title -->
            <TextBlock Text="Leaderboard"
                   Style="{ThemeResource TitleTextBlockStyle}"
                   Margin="0,0,0,10"/>

            <!-- List of students -->
            <StackPanel Orientation="Horizontal" Spacing="20" Margin="14,0,0,0">
                <TextBlock Text="#"
                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Name" 
                   Style="{ThemeResource BodyStrongTextBlockStyle}" 
                   Width="250"/>
                <TextBlock Text="# of Hours" 
                   Style="{ThemeResource BodyStrongTextBlockStyle}"/>
            </StackPanel>
            <ListView ItemsSource="{x:Bind leaderboardData.StudentData, Mode=OneWay}" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="core:LeaderboardStudent">
                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <TextBlock Text="{x:Bind Rank}"
                                       Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                            <TextBlock Text="{x:Bind Name}" 
                                   Style="{ThemeResource BodyTextBlockStyle}" 
                                   Width="250"
                                   Foreground="{x:Bind Name, Converter={StaticResource NameMatchConverter}}"/>
                            <TextBlock Text="{x:Bind Hours}" 
                                   Style="{ThemeResource BodyTextBlockStyle}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- Spinner overlay -->
        <ProgressRing IsActive="{x:Bind IsLoading, Mode=OneWay}"
                  Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Width="60" Height="60"/>
    </Grid>
</Page>