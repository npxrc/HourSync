{
	"1.9.0": [
		"The app actually starts now",
		"The app is localized into English and German now",
		"Localization works and is no longer a bunch of string names instead of their content",
		"Similar requests based on title and date submitted are grouped together on the home page",
		"Added a Trip Calculator button to the Request Maker page to easily calculate the number of eHours you should submit for a trip or event",
    "Request Maker helps you split larger requests (>99.75 eHours) into smaller requests to avoid the 100 eHour limit"
	],
	"1.8.0": [
		"New draft manager and save to OneDrive allows you to save drafts to cloud storage and load drafts. Ability to create a new draft (as in starting a new file) has not been added yet, but workaround is to start typing a new title which will create a new draft.",
		"New AppSettings file for future use. This is more like an AppData file, so the app can store data like last update check and more.",
		"Reload button added to Home so you can update the home content manually instead of having to log back into the app.",
		"Automatically convert HEIC files to PNG using ImageMagick",
		"Delete Request for a pending request now properly loads the document so it can check that a request is able to be deleted. It should already be able to delete if it's pending, but a redundancy check doesn't hurt",
		"About HourSync button maybe fixed?"
	],
	"1.5.0": [
		"Leaderboard has been released",
		"Streamlined request viewing processs",
		"BrowserView has been moved into RequestViewer",
		"Performance updates by using binding and other fixes"
	],
	"1.4.0": [
		"Minimum window sizes finally set up so app doesn't shrink too much",
		"New Image Viewer with a new zooming method",
		"Acrylic material visible in Image Viewer, may come elsewhere",
		"Theme change event listener instead of using a timer (=performance fix)",
		"Visual and bug fixes"
	],
	"1.3.0": [
		"New AI Companion for eHour requests",
		"Image selector in Submission Creator",
		"Editing pending requests is available in HourSync",
		"Implementation of HourSyncCore, an external library",
		"Bug fixes"
	],
	"1.2.0": [
		"Endpoint changes",
		"Added DLLs to ensure fix app not startup",
		"Stability and bug fixes"
	],
	"1.1.0": [
		"New settings page",
		"New leaderboard page, disabled until more data comes in.",
		"Initial settings downloads correctly now"
	],
	"1.0.0": [
		"Home page separates pending, accepted, returned, and denied requests",
		"App signs you in after inactivity",
		"Fixed auto-login"
	]
}
