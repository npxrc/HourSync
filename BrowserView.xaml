<Window
    x:Class="HourSync.BrowserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HourSync"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="BrowserView">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,*" Margin="10">
        <!-- Toolbar -->
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button x:Name="BackButton" Content="←" Width="40" />
            <Button x:Name="ForwardButton" Content="→" Width="40" />
            <Button x:Name="ReloadButton" Content="⟳" Width="40" />
            <TextBox x:Name="UrlBox" IsReadOnly="True" Width="400" />
        </StackPanel>

        <!-- WebView -->
        <wv2:WebView2 x:Name="Browser"
                      Grid.Row="1"
                      Margin="5"
                      DefaultBackgroundColor="Transparent"/>
    </Grid>
</Window>
