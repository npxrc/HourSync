<Page
    x:Class="HourSync.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--<TextBlock Text="HourSync is beta software and bugs are possible. Report bugs at https://t.ly/I1i02" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0, 10, 0, 0"/>-->

        <!-- Content for the top row -->
        <StackPanel Grid.Row="0" Margin="20, 20, 20, 5">
            <TextBlock x:Name="StudentName" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,10"/>
            <TextBlock x:Name="StudentAcademy" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,10"/>
            <TextBlock x:Name="StudentEHours" Style="{ThemeResource BodyTextBlockStyle}" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Progress to Endorsement:"/>
                <TextBlock x:Name="progressToEndorsementText" Text="0%" Style="{ThemeResource BodyStrongTextBlockStyle}" Margin="4, 0, 0, 0"/>
            </StackPanel>
            <ProgressBar x:Name="StudentEHourProgress" Margin="0,10" Width="500px" HorizontalAlignment="Left" ToolTipService.ToolTip="Your progress towards 200 eHours"/>
            <Button Content="Logout" Click="Logout_Click" Margin="0,0,0,10"/>
            <DropDownButton Content="Sort by" ToolTipService.ToolTip="Sort" AutomationProperties.Name="Sort" IsEnabled="False">
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Text="Date (Old to New)" Click="SortDateOtoN"/>
                        <MenuFlyoutItem Text="Date (New to Old)" Click="SortDateNtoO"/>
                        <MenuFlyoutItem Text="Name (A to Z)" Click="SortNameAtoZ"/>
                        <MenuFlyoutItem Text="Name (Z to A)" Click="SortNameAtoZ"/>
                        <MenuFlyoutItem Text="Hours (Low to High)" Click="SortHoursLtoH"/>
                        <MenuFlyoutItem Text="Hours (High to Low)" Click="SortHoursHtoL"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
        </StackPanel>

        <!-- Main content grid in the second row -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <!-- ScrollViewer in the first column -->
            <ScrollViewer Grid.Column="0" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <StackPanel x:Name="RequestsPanel" Margin="20 10 20 10">
                        <TextBlock Text="Pending Requests:"/>
                        <StackPanel x:Name="PendingReqsPanel" Margin="0 10 0 10"/>
                        <TextBlock Text="Accepted Requests:"/>
                        <StackPanel x:Name="AcceptedReqsPanel" Margin="0 10 0 10"/>
                        <TextBlock Text="Returned Requests:"/>
                        <StackPanel x:Name="ReturnedReqsPanel" Margin="0 10 0 10"/>
                        <TextBlock Text="Denied Requests:"/>
                        <StackPanel x:Name="DeniedReqsPanel" Margin="0 10 0 10"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
            <!-- Frame in the second column -->
            <Frame Grid.Column="1" x:Name="ContentFrame"/>
        </Grid>
    </Grid>
</Page>